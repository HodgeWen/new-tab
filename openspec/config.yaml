schema: spec-driven

context: |
  Tech stack: Vue 3.5, TypeScript, WXT, Vite, GridStack, Dexie, WebDAV
  We use Vue Composition API with <script setup>, prefer TSX/JSX
  Target platforms: Chrome, Edge, Firefox

rules:
  proposal:
    - 提案需简洁，考虑浏览器扩展的权限和存储限制
  tasks:
    - 单个任务控制在 1-2 小时内完成
