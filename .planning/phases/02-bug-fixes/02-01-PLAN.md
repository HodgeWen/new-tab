---
phase: 02-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/setting-modal/setting-modal.vue
autonomous: true

must_haves:
  truths:
    - "Setting modal opens without #imports error in dev:web mode"
  artifacts:
    - path: components/setting-modal/setting-modal.vue
      provides: "Vue imports from 'vue' not #imports"
      contains: "from 'vue'"
  key_links:
    - from: components/setting-modal/setting-modal.vue
      to: vue
      via: "import { computed } from 'vue'"
      pattern: "from ['\"]vue['\"]"
---

<objective>
Verify BUGF-01 (#imports) is resolved; commit if needed.

Purpose: WXT's #imports alias is unavailable in standalone Vite (dev:web). Research shows setting-modal.vue already uses `import { computed } from 'vue'`. Confirm no #imports in source and settings modal works in dev:web.
Output: Verified fix, committed if uncommitted.
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bug-fixes/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify #imports fix and run dev:web</name>
  <files>components/setting-modal/setting-modal.vue</files>
  <action>
1. Grep for `#imports` in `*.vue`, `*.ts`, `*.tsx` excluding `.wxt/types/`. Expect zero matches in source.
2. If any source file uses `#imports`, replace with explicit `import { ... } from 'vue'`.
3. Run `npm run dev:web` and open http://localhost:5173. Click settings icon to open the modal. Confirm no console error about #imports or module not found.
4. If verification passes, commit: `fix(02): BUGF-01 ensure no #imports in setting-modal (Vite dev:web compat)`.
  </action>
  <verify>
- `rg '#imports' --glob '*.{vue,ts,tsx}' | grep -v '.wxt/'` returns no matches in source
- `npm run dev:web` runs; settings modal opens without #imports/module error in console
  </verify>
  <done>Setting modal opens in dev:web without #imports error; fix committed if needed</done>
</task>

</tasks>

<verification>
- No #imports in source files
- dev:web settings modal opens cleanly
</verification>

<success_criteria>
BUGF-01 resolved: Setting modal opens without #imports error in dev:web mode
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-fixes/02-01-SUMMARY.md`
</output>
