---
phase: 09-grid-component-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/grid-layout/use-grid-stack.tsx
  - components/grid-layout/use-grid-stack-core.ts
  - components/grid-layout/use-grid-stack-renderer.tsx
autonomous: true
requirements:
  - GRID-02
  - GRID-03

must_haves:
  truths:
    - "`use-grid-stack.tsx` no longer mixes rendering callback internals with GridStack lifecycle logic"
    - "Core grid management and rendering logic are split into dedicated modules, each kept under ~150 lines"
    - "Global `GridStack.renderCB` mutation is encapsulated with explicit install/restore lifecycle, avoiding leaked side effects"
  artifacts:
    - path: "components/grid-layout/use-grid-stack-core.ts"
      provides: "Core GridStack lifecycle, widget CRUD, and mount/unmount orchestration"
      contains: "export function createGridStackCore"
    - path: "components/grid-layout/use-grid-stack-renderer.tsx"
      provides: "Render callback adapter and Vue vnode mount/unmount helpers"
      contains: "export function createGridStackRenderer"
    - path: "components/grid-layout/use-grid-stack.tsx"
      provides: "Thin composable entrypoint wiring core + renderer together"
      contains: "useGridStack"
  key_links:
    - from: "components/grid-layout/use-grid-stack.tsx"
      to: "components/grid-layout/use-grid-stack-core.ts"
      via: "facade composes core module"
      pattern: "createGridStackCore"
    - from: "components/grid-layout/use-grid-stack.tsx"
      to: "components/grid-layout/use-grid-stack-renderer.tsx"
      via: "facade composes renderer module"
      pattern: "createGridStackRenderer"
    - from: "components/grid-layout/use-grid-stack-renderer.tsx"
      to: "GridStack.renderCB"
      via: "install and restore callback around composable lifecycle"
      pattern: "GridStack\\.renderCB"
---

<objective>
Split `use-grid-stack.tsx` into focused modules and encapsulate render callback side effects within composable lifecycle.

Purpose: make the grid layer maintainable by separating concerns:
1) core grid management (init/events/widget CRUD),
2) rendering adapter (VNode rendering + `renderCB` lifecycle),
3) thin public composable facade.
Output: architecture aligned with `GRID-02` and `GRID-03`, behavior preserved for current UI.
</objective>

<execution_context>
@/home/whj/.codex/get-shit-done/workflows/execute-plan.md
@/home/whj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@components/grid-layout/grid-layout.vue
@components/grid-layout/use-grid-stack.tsx
@components/folder-item/folder-item.vue
@components/site-item/site-item.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract renderer adapter with explicit callback lifecycle</name>
  <files>components/grid-layout/use-grid-stack-renderer.tsx</files>
  <action>
Create `use-grid-stack-renderer.tsx` dedicated to rendering concerns only.

Implementation requirements:
1. Add `createGridStackRenderer` factory that receives read-only accessors:
   - `getGridItem(id: string): GridItemUI | undefined`
   - `renderWidget(item: GridItemUI): VNode`
2. Move `shadowDom` tracking and `render(vnode, el)`/`render(null, el)` cleanup into this module.
3. Encapsulate `GridStack.renderCB` setup via an `installRenderCallback()` method.
4. Capture previous callback before override and expose `restoreRenderCallback()` for cleanup in composable unmount.
5. Expose narrow API to core/facade:
   - `findWidgetEl(id)`
   - `renderWidgetById(id)`
   - `cleanupRemoved(nodes)`
   - `cleanupAll()`
   - `installRenderCallback()`
   - `restoreRenderCallback()`

Keep module focused: no GridStack init, no store writes, no watch/edit-mode logic.
  </action>
  <verify>`rg "GridStack\\.renderCB|restoreRenderCallback|cleanupAll" components/grid-layout/use-grid-stack-renderer.tsx` confirms side-effect install + teardown are present.</verify>
  <done>Renderer module owns callback override lifecycle and DOM render cleanup, with no leaked global side effect.</done>
</task>

<task type="auto">
  <name>Task 2: Extract grid core module and slim facade</name>
  <files>components/grid-layout/use-grid-stack-core.ts, components/grid-layout/use-grid-stack.tsx</files>
  <action>
Create `use-grid-stack-core.ts` and move all grid lifecycle / widget CRUD logic from current monolith:
- Grid init and destroy
- load/reload widgets
- add/remove/detach/attach/update/batchRemove APIs
- removed/dragstop event registration (behavior unchanged in this plan)
- edit-mode watch binding

Then rewrite `use-grid-stack.tsx` as a facade that:
1. Instantiates renderer + core modules
2. Calls `installRenderCallback()` before GridStack init
3. Calls `restoreRenderCallback()` and renderer cleanup on unmount
4. Returns the same public API currently exposed by grid-layout.vue

Refactor constraints:
- Maintain existing runtime behavior and API surface.
- Keep each new focused module around ~150 lines max (excluding blank lines/comments).
- Preserve existing type-safe narrowing (`isSiteItem`/`isFolderItem`) from Phase 8.
  </action>
  <verify>
1. `npx vue-tsc --noEmit` passes
2. `wc -l components/grid-layout/use-grid-stack-core.ts components/grid-layout/use-grid-stack-renderer.tsx` shows each file around <= 150 lines
3. `node /home/whj/.codex/get-shit-done/bin/gsd-tools.cjs verify references .planning/phases/09-grid-component-structure/09-01-PLAN.md`
  </verify>
  <done>Grid module is decomposed into focused core + renderer units, and composable facade remains stable for callers.</done>
</task>

</tasks>

<verification>
1. `npx vue-tsc --noEmit` passes with zero errors
2. `rg "GridStack\\.renderCB" components/grid-layout/use-grid-stack-renderer.tsx components/grid-layout/use-grid-stack.tsx` confirms callback wiring only through renderer lifecycle APIs
3. `wc -l components/grid-layout/use-grid-stack-core.ts components/grid-layout/use-grid-stack-renderer.tsx` confirms target module size split
4. `node /home/whj/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/09-grid-component-structure/09-01-PLAN.md`
</verification>

<success_criteria>
- Grid composable logic is split into focused core + renderer modules (maintainable structure)
- `GridStack.renderCB` side effect has explicit setup/teardown lifecycle and no leaked global mutation
- Public `useGridStack` API remains compatible with `grid-layout.vue`
- Project compiles successfully after refactor
</success_criteria>

<output>
After completion, create `.planning/phases/09-grid-component-structure/09-01-SUMMARY.md`
</output>
