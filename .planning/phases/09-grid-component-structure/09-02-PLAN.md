---
phase: 09-grid-component-structure
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - components/grid-layout/use-grid-stack-core.ts
autonomous: true
requirements:
  - GRID-04

must_haves:
  truths:
    - "Drag-stop sorting no longer writes order repeatedly for identical layouts"
    - "Order persistence is throttled/debounced to avoid burst updates during rapid drag interactions"
    - "Pending drag-stop timers are cleared on unmount to prevent stale writes"
  artifacts:
    - path: "components/grid-layout/use-grid-stack-core.ts"
      provides: "Performance-protected drag-stop order persistence"
      contains: "dragstop"
  key_links:
    - from: "components/grid-layout/use-grid-stack-core.ts"
      to: "updateGridOrder"
      via: "debounced + guarded invocation"
      pattern: "updateGridOrder"
    - from: "components/grid-layout/use-grid-stack-core.ts"
      to: "onBeforeUnmount cleanup"
      via: "clear pending timer"
      pattern: "clearTimeout|timer"
---

<objective>
Add performance protection for drag-stop sorting so order updates only persist when needed and at controlled frequency.

Purpose: satisfy `GRID-04` by preventing redundant store writes and unnecessary local persistence churn during drag operations.
Output: debounced/guarded drag-stop flow with deterministic cleanup.
</objective>

<execution_context>
@/home/whj/.codex/get-shit-done/workflows/execute-plan.md
@/home/whj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-grid-component-structure/09-01-PLAN.md
@components/grid-layout/use-grid-stack-core.ts
@store/grid-order.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drag-stop debounce pipeline</name>
  <files>components/grid-layout/use-grid-stack-core.ts</files>
  <action>
Refactor the `dragstop` handler to use a debounced commit path.

Implementation requirements:
1. Introduce a small debounce window (for example 100-160ms) dedicated to order persistence.
2. Move order extraction/sorting into a pure helper:
   - stable sort by `y`, then `x`
   - map/filter to `string[]` ids with type-safe filtering
3. In `dragstop`, schedule persistence via the debounce helper instead of immediate `updateGridOrder`.
4. Ensure timer is cancellable and stored within core module scope.

Do not alter existing sort semantics; only add write-frequency protection.
  </action>
  <verify>`rg "dragstop|setTimeout|clearTimeout" components/grid-layout/use-grid-stack-core.ts` confirms debounced scheduling exists.</verify>
  <done>Drag-stop no longer commits immediately on every event and now routes through debounce control.</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotency guard to skip duplicate order writes</name>
  <files>components/grid-layout/use-grid-stack-core.ts</files>
  <action>
Add duplicate-write guard so identical order snapshots are ignored.

Implementation requirements:
1. Keep `lastCommittedOrderSignature` (for example joined ids string) in module state.
2. Before calling `updateGridOrder(ids)`, compare current signature with last committed.
3. Only persist when signature changes.
4. Reset/cleanup transient state on grid teardown.
5. Clear pending debounce timer in unmount/destroy path to avoid post-destroy updates.

This plan focuses on write suppression and lifecycle safety; no UI behavior changes.
  </action>
  <verify>
1. `npx vue-tsc --noEmit` passes
2. `rg "lastCommitted|signature|updateGridOrder" components/grid-layout/use-grid-stack-core.ts` confirms duplicate guard logic
3. `node /home/whj/.codex/get-shit-done/bin/gsd-tools.cjs verify references .planning/phases/09-grid-component-structure/09-02-PLAN.md`
  </verify>
  <done>Order persistence runs only when order truly changes, with cleanup-safe debounced execution.</done>
</task>

</tasks>

<verification>
1. `npx vue-tsc --noEmit` passes with zero errors
2. `rg "dragstop" components/grid-layout/use-grid-stack-core.ts` confirms protected handler path
3. `rg "setTimeout|clearTimeout|lastCommitted" components/grid-layout/use-grid-stack-core.ts` confirms debounce + idempotency guards
4. `node /home/whj/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/09-grid-component-structure/09-02-PLAN.md`
</verification>

<success_criteria>
- Drag-stop order updates are performance protected (debounce/throttle equivalent)
- Redundant updates for unchanged order are skipped
- Timer/state cleanup prevents teardown-time stale writes
- Project compiles successfully after changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-grid-component-structure/09-02-SUMMARY.md`
</output>
