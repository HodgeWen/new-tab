---
phase: 09-grid-component-structure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/folder-item/folder-preview.vue
  - components/folder-item/folder-item.vue
  - components/folder-item/index.ts
autonomous: true
requirements:
  - FLDR-04

must_haves:
  truths:
    - "Folder preview grid rendering is extracted into an independent `FolderPreview` component with its own template"
    - "`folder-item.vue` delegates preview rendering to child component and keeps folder container interactions/context menu logic"
    - "Preview component is reusable in isolation via explicit props API (size + sites)"
  artifacts:
    - path: "components/folder-item/folder-preview.vue"
      provides: "Reusable folder preview grid component"
      contains: "defineProps"
    - path: "components/folder-item/folder-item.vue"
      provides: "Folder shell component delegating preview rendering"
      contains: "<n-folder-preview"
    - path: "components/folder-item/index.ts"
      provides: "Export surface including FolderPreview when needed by other modules"
      contains: "FolderPreview|NFolderPreview"
  key_links:
    - from: "components/folder-item/folder-item.vue"
      to: "components/folder-item/folder-preview.vue"
      via: "component composition for preview grid"
      pattern: "folder-preview"
    - from: "components/folder-item/folder-preview.vue"
      to: "FolderSize layout rules"
      via: "size-driven layout map"
      pattern: "horizontal|vertical|square"
---

<objective>
Extract the folder preview grid into a standalone component to improve modularity and reuse.

Purpose: satisfy `FLDR-04` by removing preview rendering logic from `folder-item.vue` and giving it a dedicated component boundary with explicit props.
Output: `FolderPreview` component with independent template/style/script and stable integration in folder item.
</objective>

<execution_context>
@/home/whj/.codex/get-shit-done/workflows/execute-plan.md
@/home/whj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@components/folder-item/folder-item.vue
@components/folder-item/shared.ts
@types/ui.ts
@types/common.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable FolderPreview component</name>
  <files>components/folder-item/folder-preview.vue</files>
  <action>
Create `folder-preview.vue` as a pure presentation component.

Implementation requirements:
1. Define explicit props for preview rendering:
   - `size: FolderSize`
   - `sites: SiteItemUI[]` (or readonly equivalent)
2. Move layout mapping (`horizontal/vertical/square`) into this component (or shared helper) and derive `cols/rows/max`.
3. Render preview anchors/icons/fallback initials inside component template.
4. Keep preview-specific styles scoped to this component.
5. Ensure no store access, no context-menu logic, no modal opening in this component.

Result should be reusable by any parent that can provide size + site list.
  </action>
  <verify>`rg "defineProps|folder-grid|horizontal|vertical|square" components/folder-item/folder-preview.vue` confirms standalone preview API and layout logic.</verify>
  <done>Folder preview rendering is independently encapsulated and reusable by props alone.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor folder-item to consume FolderPreview</name>
  <files>components/folder-item/folder-item.vue, components/folder-item/index.ts</files>
  <action>
Refactor `folder-item.vue` into composition style:
1. Remove inline preview grid template/script logic that moved to child component.
2. Keep folder shell concerns in parent:
   - open folder view on click
   - context-menu actions (edit/delete)
   - title display and container glass styling
3. Compute/pass preview sites + size into `<n-folder-preview ... />`.
4. Update `components/folder-item/index.ts` exports so consumer imports stay clear and future reuse is easy.

Do not change folder behavior; only relocate rendering responsibility.
  </action>
  <verify>
1. `npx vue-tsc --noEmit` passes
2. `rg "<n-folder-preview|NFolderPreview" components/folder-item/folder-item.vue components/folder-item/index.ts` confirms composition wiring
3. `node /home/whj/.codex/get-shit-done/bin/gsd-tools.cjs verify references .planning/phases/09-grid-component-structure/09-03-PLAN.md`
  </verify>
  <done>`folder-item.vue` is slimmer and preview rendering lives in a dedicated, reusable component.</done>
</task>

</tasks>

<verification>
1. `npx vue-tsc --noEmit` passes with zero errors
2. `rg "folder-grid|folder-icon-wrapper" components/folder-item/folder-preview.vue` confirms preview DOM/CSS moved to child
3. `rg "<n-folder-preview" components/folder-item/folder-item.vue` confirms parent delegates preview rendering
4. `node /home/whj/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/09-grid-component-structure/09-03-PLAN.md`
</verification>

<success_criteria>
- Folder preview logic has a standalone component boundary (`FolderPreview`)
- `folder-item.vue` focuses on shell interactions and context menu only
- New preview component is reusable by explicit props API
- Project compiles successfully after extraction
</success_criteria>

<output>
After completion, create `.planning/phases/09-grid-component-structure/09-03-SUMMARY.md`
</output>
