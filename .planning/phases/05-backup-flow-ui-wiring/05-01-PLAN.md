---
phase: 05-backup-flow-ui-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/setting-modal/setting-modal.vue]
autonomous: true

must_haves:
  truths:
    - "User can trigger backup export from UI and download JSON with gridItems + gridOrder"
  artifacts:
    - path: components/setting-modal/setting-modal.vue
      provides: "Backup section with export button"
      contains: "备份与恢复"
  key_links:
    - from: components/setting-modal/setting-modal.vue
      to: utils/backup.ts
      via: "import exportBackupData, button @click"
      pattern: "exportBackupData"
---

<objective>
Wire backup export from setting-modal so users can download JSON containing gridItems + gridOrder.

Purpose: BKUP-01 — export UI entry point; Phase 3 data layer complete, this closes the export flow gap.
Output: "备份与恢复" section in setting-modal with export button wired to exportBackupData.
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backup-flow-ui-wiring/05-RESEARCH.md
@utils/backup.ts
@components/setting-modal/setting-modal.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backup export section and wire to exportBackupData</name>
  <files>components/setting-modal/setting-modal.vue</files>
  <action>
Add a "备份与恢复" section to setting-modal.vue, following the WebDAV section pattern (section-header, section-title, section-desc).

1. Add section structure:
   - section-header with section-title "备份与恢复" and section-desc "导出/导入网站与布局数据"
   - A backup-actions container (similar to webdav-settings; use var(--glass-bg), var(--radius-md), var(--spacing-md) for consistency)

2. Add export button:
   - Import NButton (already imported) and exportBackupData from '@/utils/backup'
   - Add n-button with variant="secondary" (or ghost per RESEARCH) with text "导出备份"
   - Wire @click to handleExport which calls `await exportBackupData()` (no UI feedback needed; download triggers automatically)

3. Do NOT add import UI in this plan — that is Plan 05-02.

4. Style: Use existing .section-header, .section-title, .section-desc classes; add .backup-actions with same pattern as .webdav-settings (glass panel, padding, border).
</action>
  <verify>npm run dev:web; open new tab; open settings modal; click "导出备份"; verify JSON file downloads with gridItems and gridOrder keys</verify>
  <done>Export button triggers download of valid backup JSON containing gridItems + gridOrder</done>
</task>

</tasks>

<verification>
- Export button visible in settings modal
- Clicking export downloads JSON file with gridItems array and gridOrder array
- No changes to utils/backup.ts (data layer unchanged)
</verification>

<success_criteria>
[ ] User can click "导出备份" in settings modal and receive JSON file download
[ ] Downloaded JSON contains gridItems and gridOrder
</success_criteria>

<output>
After completion, create `.planning/phases/05-backup-flow-ui-wiring/05-01-SUMMARY.md`
</output>
