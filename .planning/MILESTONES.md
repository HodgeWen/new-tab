# Milestones

## v1.1.0 MVP (Shipped: 2026-02-12)

**Phases completed:** 6 phases, 11 plans, 0 tasks

**Key accomplishments:**
- 搜索栏使用 chrome.search.query API 合规，站点 URL 限制为 http/https 协议
- 修复 dev:web 模式下 #imports、JSON.parse 容错、CORS 代理等问题，清除死代码
- 备份导出包含 gridOrder，导入增加 Zod schema 验证防止数据污染
- 设置面板样式对齐设计系统 CSS 变量，WebDAV 标记为"即将推出"
- 备份导出/导入 UI 完整接入 setting-modal，含成功/错误反馈和布局刷新
- 新增设计 Token（--glass-bg-disabled、--glass-bg-elevated），组件迁移至主题变量

---

