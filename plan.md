# 重构计划

## 1. 废弃并移除`composables/useUI.ts`

- 对于全局状态现在使用 `stores/ui.ts` 的 useUIStore 去替代，这个文件中的代码已经完成了，不需要再增加东西了

- 对于弹框操作，比如网站编辑面板，文件夹编辑面板，现在基于依赖注入面板的模板引用, `utils/di.ts`文件是`key`, 目前提供了 `siteEdit` 弹框, 可以在任何子组件中注入他们，然后调用其 `open` 方法打开弹框并设置数据上下文，具体请查看 `components/site/site-edit.vue`, 所有的弹框类操作都使用这种模式!!

## 2. 让代码更加优雅

- 优化组件文件结构, 目前`components`中已经迁移了一部分了。具体就是把组件进行分组。
- 重构组件代码，可以参考`components/site/site-edit.vue`文件，这是我的编码习惯，简洁、高效、优雅。
- 重构上下文菜单组件，在 `shadcn/ui/context-menu` 文件夹中添加并导出一个api调用的方法来弹出上下文菜单，不再导出这些组件

## 3. 修复错误

我已经手动做了一些更改，但是还有很多没有完成，请帮我完成并修复错误
